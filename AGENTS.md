# happy-hunt

일상의 소소한 행복 순간을 기록하면, 그 순간에서 "특색있는 아이템"을 추출해 지급하고
그 아이템으로 펫을 키우는 발견/수집형 기록 앱. 단순 일기장이 아니라 "게임" 요소로
차별화하는 것이 핵심 컨셉.

## 앱 컨셉 & 게임 루프

1. 유저가 행복한 순간을 메모로 저장한다.
2. 메모에서 그 순간만의 특색있는 아이템을 추출한다 (예: "오늘", "행복", "친구" 같은
   뻔한 단어가 아니라 "치킨", "노을", "된장찌개" 같은 구체적 아이템).
3. 추출된 아이템을 유저에게 지급하고, 아이템으로 펫을 키운다.
4. 펫은 다마고치 스타일 — 최근 기록량/감정에 따라 표정과 상태가 실시간으로 변한다.
5. 획득한 아이템은 보관함에 쌓인다 (수집 요소).

경쟁 벤치마크: 행복저금통 (단순 기록형). happy-hunt는 발견/수집형으로 차별화한다.

향후 확장 후보: 감정 이름 붙이기 — 사용자가 직접 붙이거나, 보기를 제공하거나,
"정말 모르겠어요" 버튼을 누르면 AI가 감정 이름을 제안.

## 도메인 용어집

에이전트가 코드/커밋/이슈에서 아래 한국어 도메인 용어를 만나면 이 정의를 기준으로 삼는다.

- **행복 메모**: 유저가 저장하는 원본 텍스트 기록.
- **아이템 추출**: 메모에서 키워드를 뽑아 그 순간을 대표하는 구체적 사물/장소/자연 등으로
  변환하는 과정. 카테고리: 음식 / 장소 / 자연 / 사물 / 사람_동물 / 활동 / 기타
  (`poc/extract-items.ts` 참고).
- **아이템**: 추출 결과물. 유저 보관함에 쌓이고, 펫 성장에 쓰이며, 각 아이템은 나중에
  고유 아이콘 이미지를 갖는다.
- **펫**: 다마고치 스타일 캐릭터(펫 + 사람 컨셉). 기록량/감정에 따라 실시간으로
  표정/상태가 변한다.

## 시스템 파이프라인 (계획)

```
메모 저장 → (비동기) LLM 아이템 추출 → 아이템 지급 → 아이콘 이미지 생성 → 보관함 반영
                                              └→ 펫 상태/집계 업데이트
```

메모 저장과 LLM 추출 처리는 의도적으로 분리한다 (Kafka/메시징 기반 비동기 이벤트 처리).
아이콘 생성은 별도 2단계 파이프라인으로 본다: (1) 키워드/아이템 추출 → (2) 아이템명으로
아이콘 이미지 생성 (text-to-image, 후보: DALL·E 3 / Stable Diffusion. SD는 라이선스 자유도와
스타일 파인튜닝(LoRA) 여지 때문에 유력 후보).

## 기술적 챌린지 (로드맵 겸 설계 제약)

이벤트/파이프라인 관련:
- Kafka/메시징 기반 비동기 이벤트 처리로 메모 저장과 LLM 추출을 분리
- 재시도 + DLQ(Dead Letter Queue) 설계
- 컨슈머 중복 소비 대비 멱등성 처리
- 배치/집계 파이프라인 (펫 상태, 주간 통계)
- Circuit Breaker로 LLM API 장애 격리

아이콘 생성 관련:
- 스타일 일관성 문제 해결 — 프롬프트 템플릿 고정, 필요시 LoRA/스타일 파인튜닝
- 생성 결과 검증 자동화 — 이미지 사이즈/바운딩 체크, 팔레트 이탈 시 재생성,
  CLIP 기반 유사도 스코어로 "의도한 사물에 맞는지" 검증
- 생성 큐 + 캐싱 전략
- 비용 최적화 — 동적 생성 vs 고정 세트 하이브리드
- 실패 처리 — Circuit Breaker

새 기능을 설계/구현할 때 이 제약들을 무시하고 단순 동기 처리로 구현하지 말 것 —
초기 기획 단계부터 비동기/장애격리를 전제로 하는 프로젝트다. 다만 poc 단계 코드는
예외로, 파이프라인 없이 개념만 검증해도 된다.

## 현재 상태

- 코드베이스는 NestJS 기본 스캐폴드 단계이며, 실제 기능 모듈은 아직 없다
  (`src/`에는 `app.module.ts` 하나뿐).
- `poc/` 디렉토리에 아이템 추출 로직의 개념 검증(POC) 스크립트가 진행 중이다.
  로컬 LLM(Ollama)으로 아이템 추출이 신뢰할 수 있는 JSON 스키마로 나오는지 검증하는
  단계이며, 아직 NestJS 앱에 통합되지 않았다. 프로덕션 코드가 아니므로 `src/`와 같은
  기준으로 리뷰하지 않는다.
- Kafka/DLQ/배치 파이프라인, 아이콘 생성 파이프라인은 아직 미착수 — 위 챌린지 목록은
  설계 시 참고할 제약사항이지, 이미 구현된 것이 아니다.

## 기술 스택

- NestJS + TypeScript, ESM (`"type": "module"`)
- 테스트: vitest (`test:e2e`는 별도 config)
- 린트/포맷: oxlint, prettier
- 스키마 검증: zod
- POC 스크립트는 `--experimental-strip-types`로 트랜스파일 없이 직접 실행 (`node poc/*.ts`)

## 자주 쓰는 커맨드

```bash
npm run start:dev     # 개발 서버 (watch)
npm run lint           # oxlint src/ test/
npm run test           # vitest 단위 테스트
npm run test:e2e       # e2e 테스트
npm run format          # prettier
```

코드 변경 후에는 `npm run lint`와 `npm run test`를 실행해 확인한다.

## 구조 원칙

- 기능이 늘어나면 NestJS 컨벤션에 따라 기능별 모듈 폴더로 나눈다
  (예: `src/happy-memo/`, `src/item-extraction/`, `src/pet/`). 파일 하나만 봐도 그
  모듈의 책임을 파악할 수 있게 유지한다.
- `poc/`는 실험 전용 디렉토리다. 검증이 끝난 로직만 `src/`의 정식 모듈로 승격한다.

## Git 작업 규칙

- 저장소 상태를 바꾸는 git 명령(`add`, `commit`, `push`, `merge`, `branch`, `checkout`
  으로 브랜치 이동 등)은 사용자에게 먼저 확인받고 실행한다. 임의로 커밋하거나
  origin에 푸시하지 않는다.
- `status`, `diff`, `log` 같은 읽기 전용 명령은 확인 없이 사용해도 된다.
- 사용자가 이미 "커밋해줘", "머지해줘"처럼 특정 작업을 명시적으로 요청한 경우에만
  해당 범위 내에서 실행한다. 요청받지 않은 범위(예: 커밋만 요청했는데 push까지)로
  확장하지 않는다.
